## UI 界面全面分析

- 对项目 UI 进行了系统性分析，产出 `docs/ui_analysis.md`
- **设计系统**: 基于 v8 "Aurora Cockpit" 设计语言（Ninebot 蓝本），翡翠绿 `#00C896` 主色，完整 6 维 Design Token 体系
- **关键发现**: v8 ControlCard 只是纯视觉壳（onPowerOn/onSeatOpen 等回调未接入），旧的 SlideToAction + 按钮网格仍在下层实际执行控车指令

## 控车页 v8 迁移完成

- **删除旧控件**: `_ControlArea`/`_ControlAreaState`（~580 行）、`_OfficialMainControlCard`、`_PrimaryPowerControl`、`_OfficialSmallControlButton`、`_MainControlEditPage`、`_ControlTipBar`、`_EditableListRow`
- **迁移到 `_HomeTopSectionState`**: 完整的控车执行管道（ControlCommandExecutor + confirmation guard + 策略检查），长按旋钮→通电/熄火，座桶按钮→开座桶，感应解锁→ManualModeService
- **`_ControlTipBar` 内联**: 连接通道/状态信息直接嵌入 `_HomeTopSection` 底部（紧凑圆角条）
- **状态**: flutter analyze 0 error，0 warning
- **热修 (f03447c)**: CI 格式检查失败 + flutter analyze warning → dart format + 移除闲置 super.key
- **热修 (1671d18)**: 长按开机无反应 → `_PowerKnob` 从 GestureDetector(onPanStart) 改为 Listener(onPointerDown)，绕过 SingleChildScrollView 手势竞技场
